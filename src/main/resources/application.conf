ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.davideagostini.ApplicationKt.module ]
    }
}
jwt {
    domain = "http://0.0.0.0:8080/"
    audience = "jwt-audience"
    realm = "ktor sample app"
    secret = "mySecretToMaintainSecret32"
}
